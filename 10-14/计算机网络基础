1、按缓存位置：
  1、Service Work：两种情况导致混村清除：
    手动调用API： cache-delete(resource)或者内容超出限制
  2、Memory-cache: 会话关闭即清除
    改机制保证了一个页面中如果有两个相同请求都实际只会被请求一次，例如两个src相同的图片或者两个href相同的link
  
  3、Disk-cache: 根据http 请求头中的信息来判定哪些资源可以缓存

  4、如果以上三个位置都没有找到缓存，就会发送请求，返回的请求也会把资源添加到缓存中

按失效策略
  1、强制缓存
    可以造成强制缓存的字段时：Cache-control 和 Expire
    · Expire：HTTP 1.0, 是绝对时间，因为客户端可以修改本地时间，所以有误差
    · Cache-control：HTTP 1.1，Max-age：设定最长有效时间

    命中则直接使用 缓存

  2、协商缓存
    协商缓存在请求数量上和没有缓存是一致的，但是如果返回304就不会返回实际文件内容，因此节省体积
    · Last-Modified & if-Modified-Since
      服务器通过 Last-Modified 字段告知客户端资源最后一次更新的时间
      如果更新了 返回200 -> 并返回相应资源，
      没有更新： 返回304 -> 使用本地缓存
    · Etag 服务端储存的文件的特殊标识，流程相同对比 Etag，修改了就返回新的资源，否则使用本地资源
    Etag优先级高于 LastModified

2、CDN 原理
  原理是采用各种缓存服务器，将这些缓存服务器分不到访问相对集中的区域或者网路中，利用负载均衡技术将用户
  的访问指向距离最近的缓存服务器上。

3、DNS 解析
  浏览器缓存：浏览器会按照一定的频率缓存 DNS 记录。
  操作系统缓存：如果浏览器缓存中找不到需要的 DNS 记录，那就去操作系统中找。
  路由缓存：路由器也有 DNS 缓存。
  ISP 的 DNS 服务器：ISP 是互联网服务提供商(Internet Service Provider)的简称，ISP 有专门的 DNS 服务器应对 DNS 查询请求。
  根服务器：ISP 的 DNS 服务器还找不到的话，它就会向根服务器发出请求，进行递归查询（DNS 服务器先问根域名服务器.com 域名服务器的 IP 地址，然后再问.baidu 域名服务器，依次类推）

4、OSI 七层模型
  应用层：文件传输，常用协议： HTTP
  表示层：数据格式化，代码转换，数据加密
  会话层：简历解除会话
  传输层：提供端对端的接口：TCP
  网络层：为数据包选择路由： IP，icmp
  数据链路层：传输有地址的帧
  物理层：二进制数据形式在屋里媒体上的数据传输
